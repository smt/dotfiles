# ~/.tmuxinator/dawn.yml
# you can make as many tabs as you wish...

project_name: dawn
project_root: ~/code/empathylab/dawn
windows:
  - editor: vim
  - shell:
      layout: main-vertical
      panes:
        - git pull
        - #empty, will just run plain zsh
        - #empty, will just run plain zsh
